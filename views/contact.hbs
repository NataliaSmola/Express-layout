<!DOCTYPE html>
<html lang="en" dir="ltr">
  <meta charset="utf-8">
  <title>Contact</title>
  <h2>Contact me</h2>
  {{#if isSent}}
  <div class="isSent">The message has been sent! {{imageName}} has been saved </div>
  {{/if}}
  {{#if isError}}
  <div class="isError">The message has not been sent! {{imageName}} has been saved</div>
  {{/if}}
<form action="/contact/send-message" method="post" enctype="multipart/form-data">
  <label>
    Your name: <input type="text" name="author">
  </label>
  <label>
    Your email: <input type="email" name="sender">
  </label>
  <label>
    Title: <input type="text" name="title">
  </label>
  <label>
    Project design: <input type="file" accept=".png, .jpg, .jpeg, .gif" name="image" required/>
  </label>
   <label>
    Message: <textarea name="message"></textarea>
  </label>
  <button type="submit">Send message</button>
</form>
</html>
